<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhoujian.motionlayoutdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ObjectAnimatorActivity" />
        <activity android:name=".activity.ObjectAnimator2Activity" />
        <activity android:name=".activity.Go" />
        <activity android:name=".activity.ConstraintSet" />
        <activity android:name=".activity.MyMotionLayoutActivity" />
        <activity android:name=".activity.SampleActivity" />
        <activity android:name=".activity.YoutubeActivity" />
        <activity android:name=".activity.MotionLayoutActivity" />
        <activity android:name=".activity.HenCoderActivity" />
    </application>

</manifest>